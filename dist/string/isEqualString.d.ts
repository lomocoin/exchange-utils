declare const _default: (firstId: string | number | undefined, secondId: string | number | undefined) => boolean;
/**
 * Convert two value to string and compare it to see if are the same
 */
export default _default;
